# Published digest

Stories the owner chose to publish, keeping their draft file names and `id`s. Fix
errors in place and add a dated correction line at the end instead of silently
changing a published claim.
